{% extends 'layout.html.twig' %} 

{% block title %} 
{{ parent() }} -accueil {%
endblock %}

{% block content %}
<div class="row mt-5 justify-content-center text-center">
  <h1>Dernier article publié</h1>
  <div>
    <h3>{{dernierArticle.titre}}</h3>
    <h6>publié le {{dernierArticle.DateDeCreation| date("d/m/Y \à H\\hi")}}</h6>
    {% if dernierArticle.dateDeModification is not null %}
 <h6 class="text-muted">dernière modification le: {{dernierArticle.DateDeModification| date("d/m/Y \à H\hi")}}</h6>
    <h6>auteur de l'article</h6>
    {% endif %}
    <p>{{dernierArticle.contenu | slice(0,150)}}....<a href=""> lire la suite</a></p>
  </div>
</div>

{% endblock content %}
